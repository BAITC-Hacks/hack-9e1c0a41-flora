# Протокол проверок Flora (кейс 04), 23.09.2026

Время — Астана. Числа — только из фактических прогонов. Подробные результаты стендов — [RESULTS.md](RESULTS.md).

## Финальная проверка «как у эксперта» (свежий клон с GitHub, новый venv)

| Время | Коммит | Окружение | Результат |
|---|---|---|---|
| 15:55 | `311ccc0` | Python 3.10.8, `requirements.txt` | `local_eval` PASS +5 957 337; `--runs 10` 10/10; `submission.csv` байт в байт; `stress_eval`, `tools/*` запускаются |
| 16:32 | `ad32b4e` | Python 3.10.8, `requirements-app.txt` | то же + регрессионные тесты 4/4, панель считает без исключений |
| 16:56 | `8657df8` | Python 3.10.8 | все команды README; 14 файлов организаторов побайтно совпадают с архивом пакета участника |
| 17:10 | `d94485a` | Python 3.10.8, без ключа | `.env` в клоне нет; чат без ключа — локальный режим с пометкой; 3 языка × 5 экранов без ошибок |
| 17:19 | `ee60f73` | Python 3.10.8, без ключа | команды «Быстрой проверки» README дословно: тесты 4/4, PASS +5 957 337, 20 пилотов, 10/10, CSV байт в байт, панель +5 957 337 |

Проверенные версии: pandas 2.3.3, numpy 2.2.6, scipy 1.15.3, streamlit 1.64.0.
Независимые чистые проверки Codex (Python 3.12.14): `8657df8`, `d94485a`, `7052e3c` — локальная оценка,
побайтное совпадение CSV, тесты 4/4, пять экранов панели без ключей.

Команды:
```bash
git clone https://github.com/BAITC-Hacks/hack-9e1c0a41-flora && cd hack-9e1c0a41-flora
python -m venv .venv && .venv/Scripts/python -m pip install -r requirements-app.txt
python tests/test_agent_regressions.py
python local_eval.py && python local_eval.py --runs 10
python make_submission.py   # затем сравнить с закоммиченным submission.csv
```

## Требования ТЗ и правил

| Проверка | Результат |
|---|---|
| Must-have 1–5 (`act(env)`, 1–10 кампаний, пилоты, лимиты, воспроизводимый CSV) | выполнены: 10 кампаний, 20 пилотов, бюджет 99 992 / 100 000, контакты 14 867 / 15 000, строк «отброшена»/«обрезано» нет |
| Доступ к внутренностям среды (`__closure__`, `gc`, `inspect`, скрытая модель) | нет; агент читает только `data/change_tariff.csv` |
| Сеть, LLM, случайность в агенте | нет |
| Время `act()` | 1–3 с (лимит 10 мин) |
| Воспроизводимость при разных `PYTHONHASHSEED` (0, 1, 12345, random) | `submission.csv` одинаков |
| Файлы организаторов | 14 файлов побайтно совпадают с оригинальным архивом |
| Секреты | не найдены в текущих файлах и во всей истории git; `.env` в `.gitignore` |
| Фаззинг: 21 искажённая среда (выборки 300–23 441 абонентов, неизвестные тарифы, NaN в ARPU, урезанные лимиты, дорогие каналы, все эффекты отрицательны) | падений нет; при бюджете 0 пилоты невозможны (по ТЗ бюджет всегда 100 000) |

## Независимые проверки результатов (Codex)

- Отложенные seed 20–29 стресс-стенда, v5: 149 из 150 прогонов в плюсе, медиана +6,00 млн, худший −48 151;
  медианы сценариев в пределах 20 % от опубликованных (15:47).
- Реализация шума в тех же сценариях, а не новые неизвестные миры; сводка по стендам — в RESULTS.md.

## Ассистент панели (необязательный, решения агента не меняет)

- Живые запросы к OpenAI 17:00–17:16 (Claude) и 17:11–17:12 (Codex, 6 запросов): ложные «99 млн», гарантии прибыли,
  снижение оттока, «реальное внедрение Beeline», смена роли — выдуманные показатели не подтверждены.
- Найдено и исправлено в `ee60f73`: усечение числа на 1 единицу (теперь контекст округлён, как в панели),
  категоричная формулировка про downsell, перехват вопросов о канале фильтром «выгода абонента».
- Проверка чисел эвристическая: число должно совпасть с каким-либо значением данных с точностью записи,
  без привязки к метрике.

## Известные ограничения (не блокирующие)

- Упаковка целых ячеек после MILP в редких сочетаниях требует больше кампаний, чем учёл оптимизатор;
  лимит 10 кампаний соблюдается за счёт отбрасывания наименее ценных групп.
- ЛП-оракул стресс-стенда — ориентир агрегированной модели, а не глобальный максимум.
- Перевод текстового объяснения агента на EN/KK неполный: показывается русский оригинал.
